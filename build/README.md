# Build

Run from the workspace root:

```powershell
node build/build-ledger18.js
```

Output:

`dist/Ledger_18_dynamic_stocks_v18_35_built.html`

The build script inlines CSS and JavaScript in the order listed by `index.html`.
