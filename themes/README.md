# Themes

`ledger-dark.css` contains the current dark command-center theme tokens.

Future theme files should change variables and broad surface styling only. Gameplay layout rules belong in `styles/`.
